import { NgxNatLinkDirective } from './ngx-nat-link.directive';

describe('NgxNatLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxNatLinkDirective();
    expect(directive).toBeTruthy();
  });
});
